АЛГОРИТМ Подсчет_Объектов_Через_Углы
ВХОД: BinaryImage (матрица 0 и 1, где 1 - объект, 0 - фон)
ВЫХОД: ObjectCount (целое число)

НАЧАЛО
    1. Получить размеры изображения: Height, Width
    2. Инициализировать счетчики:
       external = 0  (внешние углы)
       internal = 0  (внутренние углы)
    
    3. Для y от 0 до Height - 2:
        Для x от 0 до Width - 2:
            
            # Извлекаем окно 2x2
            p1 = BinaryImage[y, x]         # Верхний-левый
            p2 = BinaryImage[y, x + 1]     # Верхний-правый
            p3 = BinaryImage[y + 1, x]     # Нижний-левый
            p4 = BinaryImage[y + 1, x + 1] # Нижний-правый
            
            # Суммируем количество пикселей объекта в окне
            sum_pixels = p1 + p2 + p3 + p4
            
            ЕСЛИ sum_pixels == 1 ТО
                # 1 объект, 3 фона -> Внешний угол
                external += 1
            ИНАЧЕ ЕСЛИ sum_pixels == 3 ТО
                # 3 объекта, 1 фон -> Внутренний угол
                internal = int_corners + 1
            КОНЕЦ ЕСЛИ
            
        Конец цикла x
    Конец цикла y
    
    4. Вычисляем количество объектов по формуле Эйлера
       ObjectCount = (external - internal) / 4
    
    5. Вернуть ObjectCount
КОНЕЦ 
